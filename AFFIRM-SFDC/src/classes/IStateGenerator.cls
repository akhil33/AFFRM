/**
 *
 *
 * IStateGenerator is our template for Classes that will query data within a Trigger context.
 * Implementations of this class will query data and store it in DataState.
 *
 * DataStateKeys should be defined as Static Final strings and returned  in dataStateSearchKeys
 *
 * Concrete Implementations of this interface should follow this naming Pattern:
 * [SObjectName]State_[QueriedSObjectName]. This will make is easy to know what Object
 * is queried and set by the implementation
 */

public interface IStateGenerator {
	/**
	 * @return List<String> of all the DataStateKeys that are generated by this class.
	 */
	List<String> dataStateSearchKeys();

	/**
	 * The implementation of the State Generation, this should query some table and set the State
	 * for the Keys in dataStateSearchKeys
	 */
	void generateState();
}